using UnityEngine;

public class RotatingCamera : MonoBehaviour
{
    void Update()
    {
        if (Input.GetButtonDown("RotateCamera"))
        {
            //Rotate Camera based on Mouse or joystick Input
        }
    }
}
